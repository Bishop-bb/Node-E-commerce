<%- include ('../includes/admin/header.ejs') %>

<title>Bishop: <%= title %> </title>
</head>
<body>
  <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <div class="container-fluid">
      <div class="collapse navbar-collapse" id="navbarNav">
        <ul class="navbar-nav me-auto text-white">
          <li class="nav-item">
            <a class="nav-link active" aria-current="page" href="/addUser"><i class="fas fa-sign-in-alt"></i>
                Register</a>
          </li>
         
          <!-- <li class="nav-item">
            <a class="nav-link" href="/adminProductHome"><i class="fas fa-box-open"></i>Products</a>
          </li> -->
          <!-- <li class="nav-item">
            <a class="nav-link" href="/addUser"><i class="fa-solid fa-user-plus"></i>Add-User</a>
          </li> -->
        </ul>
        <!-- <ul class="navbar-nav ms-auto text-white">
          <li class="nav-item">
            <a class="nav-link" href="/addUser"><i class="fa-solid fa-user-plus"></i>Update Details</a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="addProduct"><i class="fas fa-plus-square"></i>
              Log out</a>
          </li>
        </ul> -->
      </div>
    </div>
  </nav>
<div style="display: flex; justify-content: center;">
    <div class="container" style="width: 50%;">
        <div class="row">
            <div class="col-md-6 offset-md-3 mt-4">
                    <h2 style="text-align: center;"> Add User</h2>
                  
                
                       <div class="card-body">

                        <form action="/addUser" method="post">
                      <div class="mb-3">
                        <label for="title" class="form-label">Name</label>
                        <input  type="text" value="<%= previousInput.length >0 && previousInput[0].name ? previousInput[0].name : ''  %>" class="form-control"  name="name" placeholder="Name">
                        <% if (errors.length > 0) {
                          for(let error of errors){
                              if(error.param == "name"){ %>
                                  <strong><%= error.msg %></strong>
                       
                              <% } %>
                        <% }
                      }%>
                      </div>
                      
                      <div class="mb-3">
                        <label for="image" class="form-label">Email</label>
                        <input type="text" class="form-control"  name="email" placeholder="Email ">
                        <% if (errors.length > 0) {
                          for(let error of errors){
                              if(error.param == "email"){ %>
                                  <strong><%= error.msg %></strong>
                       
                              <% } %>
                        <% }
                      }%>
                        <strong><%= dbError %></strong>
                      </div>

                      <div class="mb-3">
                        <label for="price" class="form-label">Password</label>
                        <input type="text" class="form-control"  name="password" placeholder="Password">
                        <% if (errors.length > 0) {
                          for(let error of errors){
                              if(error.param == "password"){ %>
                                  <strong><%= error.msg %></strong>
                       
                              <% } %>
                        <% }
                      }%>
                      </div> 

                      
                       <input type="submit" class="btn btn-primary" value="Submit">
                    </form>
                      </div>
            </div>
            </div>
        </div>
    </div>


  <%- include ('../includes/admin/footer.ejs') %>
